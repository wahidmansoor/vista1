import PlaceholderPage from '../components/PlaceholderPage';

const LabsPage = () => (
  <PlaceholderPage 
    title="Important Labs Reference" 
    description="Reference values, tumor marker interpretations, and critical lab alerts." 
  />
);

export default LabsPage;
